<Page
    x:Class="TaskOrg.TPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TaskOrg"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="LayoutRoot">

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid>
            <StackPanel>
                <TextBox x:Name="TaskTitle" Text="Title" Background="#FF9B3B3B" TextAlignment="Center" VerticalAlignment="Top" TextChanged="SetTitle" Margin="10,0"/>
                <TextBox x:Name="Description" VerticalAlignment="Stretch" Height="284" Background="#FFCDC9C9" TextChanged="SetDescription" Margin="10,20,10,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
